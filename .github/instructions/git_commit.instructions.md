# Git Commits

- Keep commits small and focused.
- Check the working tree before you stage files.
- Use clear conventional-style messages.
- Never commit secrets, tokens, or machine-local config.

Examples:

```bash
git status --short
git add src/lattice_trade_kit
git commit -m "refactor: rename public package layout"
```
