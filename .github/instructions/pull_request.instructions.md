# Pull Requests

- Open pull requests from a focused branch with one main purpose.
- Use `gh` for creation, review, and status checks.
- Include tests, risk notes, and any migration impact in the description.
- Link the related issue when there is one.

Example:

```bash
gh pr create --fill
gh pr view --web
```
