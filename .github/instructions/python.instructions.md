# Python

- Use Python 3.12.x with Poetry.
- Import the public package as `lattice_trade_kit`.
- Keep framework code out of runtime modules.
- Prefer small modules, explicit types, and simple dataclasses where they help.

Example:

```bash
poetry run python -m pytest
```
